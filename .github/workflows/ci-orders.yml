name: CI - Orders

on:
  push:
    branches:
      - main
    paths:
      - gc-industries-devops-superheros/services/orders/**
      - .github/workflows/ci-orders.yml

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: gc-industries-devops-superheros

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push Orders Image
        run: |
          IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/superheros-orders

          docker build -t $IMAGE:latest services/orders
          docker push $IMAGE:latest
